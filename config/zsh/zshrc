#═══════════════════════════════════════════════════════════════════════
# GOTHAM SYSTEM — ZSH Configuration
# Modular shell environment with tactical enhancements
#═══════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────
# GOTHAM DIRECTORY
# ───────────────────────────────────────────────────────────────────────

export GOTHAM_DIR="${GOTHAM_DIR:-$HOME/gotham}"

# ───────────────────────────────────────────────────────────────────────
# LOAD CORE MODULES (in order)
# ───────────────────────────────────────────────────────────────────────

for module in "$GOTHAM_DIR/config/zsh/core"/*.zsh(N); do
    source "$module" 2>/dev/null || echo "Failed to load: $module"
done

# ───────────────────────────────────────────────────────────────────────
# LOAD PLUGINS
# ───────────────────────────────────────────────────────────────────────

if [[ -f "$GOTHAM_DIR/config/zsh/plugins/tools.zsh" ]]; then
    source "$GOTHAM_DIR/config/zsh/plugins/tools.zsh"
fi

# ───────────────────────────────────────────────────────────────────────
# LOAD ALIASES
# ───────────────────────────────────────────────────────────────────────

if [[ -f "$GOTHAM_DIR/config/zsh/aliases.zsh" ]]; then
    source "$GOTHAM_DIR/config/zsh/aliases.zsh"
fi

# ───────────────────────────────────────────────────────────────────────
# LOAD FUNCTIONS
# ───────────────────────────────────────────────────────────────────────

if [[ -f "$GOTHAM_DIR/config/zsh/functions.zsh" ]]; then
    source "$GOTHAM_DIR/config/zsh/functions.zsh"
fi

# ───────────────────────────────────────────────────────────────────────
# COMPLETION SYSTEM
# ───────────────────────────────────────────────────────────────────────

# Note: Completion is configured in core/03-completion.zsh

# ───────────────────────────────────────────────────────────────────────
# END OF CONFIGURATION
# ───────────────────────────────────────────────────────────────────────
