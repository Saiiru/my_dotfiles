[tasks.test]
description = "Roda testes Go"
run = "go test ./..."

[tasks.build]
description = "Build do binário principal"
run = "go build ./cmd/server"

[tasks.run]
description = "Roda o servidor localmente"
run = "go run ./cmd/server"
